import 'package:flutter/material.dart';

class FavorietScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text("FavorietScreen"),
      ),
    );
  }
}
